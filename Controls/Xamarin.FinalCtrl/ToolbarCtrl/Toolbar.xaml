<?xml version="1.0" encoding="UTF-8"?>
<Grid xmlns="http://xamarin.com/schemas/2014/forms"
      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
      x:Class="Xamarin.FinalCtrl.Toolbar">

    <ContentView HorizontalOptions="StartAndExpand"
                 x:Name="_left">
        <ContentView.GestureRecognizers>
            <TapGestureRecognizer Tapped="HandleLeftCtrlTapped" />
        </ContentView.GestureRecognizers>
    </ContentView>

    <Label x:Name="_lbl"
           HorizontalOptions="CenterAndExpand"
           HorizontalTextAlignment="Center"
           VerticalOptions="CenterAndExpand"
           VerticalTextAlignment="Center"
           Padding="10"
           FontAttributes="Bold" />

    <ContentView HorizontalOptions="EndAndExpand"
                 x:Name="_right">
        <ContentView.GestureRecognizers>
            <TapGestureRecognizer Tapped="HandleRightCtrlTapped" />
        </ContentView.GestureRecognizers>
    </ContentView>

</Grid>